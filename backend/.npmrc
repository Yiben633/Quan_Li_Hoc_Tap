# OpenAI v5 declares an optional Zod v3 peer while StudyFlow uses Zod v4.
# The application does not use OpenAI's Zod helpers; retain the locked versions
# so Vercel can install the backend dependency tree consistently.
legacy-peer-deps=true
