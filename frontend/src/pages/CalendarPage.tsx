import { ChevronLeft, ChevronRight, Clock3, Edit3, Plus, Trash2, X } from 'lucide-react'
import { useMemo, useState } from 'react'
import toast from 'react-hot-toast'
import { Button, EmptyState, Modal, Select, Skeleton } from '../components/ui'
import { getApiErrorMessage } from '../features/auth/auth.api'
import type { CalendarItem, CalendarView } from '../features/calendar/calendar.api'
import { useCalendarEventMutation, useCalendarQuery } from '../features/calendar/calendar.hooks'

const labels: Record<CalendarItem['type'], string> = { schedule: 'Lịch học', event: 'Sự kiện', task_due: 'Hạn task', exam: 'Kỳ thi' }
const weekdays = ['T2', 'T3', 'T4', 'T5', 'T6', 'T7', 'CN']
function dayKey(date: Date) { return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, '0')}-${String(date.getDate()).padStart(2, '0')}` }
function parseDate(value: string) { const [year, month, day] = value.split('-').map(Number); return new Date(year, month - 1, day) }
function dateTimeKey(value: string) { const date = new Date(value); return dayKey(date) }
function addDays(date: Date, amount: number) { const next = new Date(date); next.setDate(next.getDate() + amount); return next }
function monday(date: Date) { const day = date.getDay(); return addDays(date, -(day === 0 ? 6 : day - 1)) }
function monthCells(anchor: Date) { const first = new Date(anchor.getFullYear(), anchor.getMonth(), 1); const start = monday(first); return Array.from({ length: 42 }, (_, index) => addDays(start, index)) }
function timeLabel(value: string) { return new Intl.DateTimeFormat('vi-VN', { hour: '2-digit', minute: '2-digit', hour12: false, timeZone: 'Asia/Ho_Chi_Minh' }).format(new Date(value)) }

export function CalendarPage() { const [view, setView] = useState<CalendarView>('month'); const [anchor, setAnchor] = useState(() => new Date()); const [modalOpen, setModalOpen] = useState(false); const [selected, setSelected] = useState<CalendarItem | null>(null); const [editingEvent, setEditingEvent] = useState<CalendarItem | null>(null); const [draggedEvent, setDraggedEvent] = useState<CalendarItem | null>(null); const [form, setForm] = useState({ title: '', date: dayKey(new Date()), start: '09:00', end: '10:00' }); const date = dayKey(anchor); const query = useCalendarQuery(view, date); const mutations = useCalendarEventMutation(); const cells = useMemo(() => view === 'month' ? monthCells(anchor) : view === 'week' ? Array.from({ length: 7 }, (_, index) => addDays(monday(anchor), index)) : [anchor], [anchor, view]); const byDay = useMemo(() => { const map = new Map<string, CalendarItem[]>(); (query.data?.items ?? []).forEach((item) => { const key = dateTimeKey(item.startAt); map.set(key, [...(map.get(key) ?? []), item]) }); return map }, [query.data]); const move = (amount: number) => setAnchor((current) => view === 'month' ? new Date(current.getFullYear(), current.getMonth() + amount, 1) : addDays(current, view === 'week' ? amount * 7 : amount)); const openCreate = (selectedDate: Date) => { setSelected(null); setEditingEvent(null); setForm({ title: '', date: dayKey(selectedDate), start: '09:00', end: '10:00' }); setModalOpen(true) }; const save = (event: React.FormEvent) => { event.preventDefault(); const startAt = `${form.date}T${form.start}:00+07:00`; const endAt = `${form.date}T${form.end}:00+07:00`; if (editingEvent) { mutations.update.mutate({ id: editingEvent.sourceEntity.id, input: { title: form.title.trim(), startAt, endAt } }, { onSuccess: () => { setModalOpen(false); setEditingEvent(null); toast.success('Đã cập nhật sự kiện') }, onError: (error) => toast.error(getApiErrorMessage(error, 'Không thể cập nhật sự kiện')) }) } else mutations.create.mutate({ title: form.title.trim(), startAt, endAt }, { onSuccess: () => { setModalOpen(false); toast.success('Đã thêm sự kiện') }, onError: (error) => toast.error(getApiErrorMessage(error, 'Không thể thêm sự kiện')) }) }; const editSelected = () => { if (!selected || selected.type !== 'event') return; const start = new Date(selected.startAt); const end = new Date(selected.endAt); setEditingEvent(selected); setForm({ title: selected.title, date: dateTimeKey(selected.startAt), start: `${String(start.getUTCHours()).padStart(2, '0')}:${String(start.getUTCMinutes()).padStart(2, '0')}`, end: `${String(end.getUTCHours()).padStart(2, '0')}:${String(end.getUTCMinutes()).padStart(2, '0')}` }); setSelected(null); setModalOpen(true) }; const dropEvent = (target: Date) => { if (!draggedEvent || draggedEvent.type !== 'event') return; const oldStart = new Date(draggedEvent.startAt); const oldEnd = new Date(draggedEvent.endAt); const startAt = new Date(Date.UTC(target.getFullYear(), target.getMonth(), target.getDate(), oldStart.getUTCHours(), oldStart.getUTCMinutes())).toISOString(); const endAt = new Date(Date.parse(startAt) + Math.max(30 * 60000, oldEnd.getTime() - oldStart.getTime())).toISOString(); mutations.update.mutate({ id: draggedEvent.sourceEntity.id, input: { startAt, endAt } }, { onSuccess: () => toast.success('Đã đổi thời gian sự kiện'), onError: (error) => toast.error(getApiErrorMessage(error, 'Không thể đổi thời gian sự kiện')) }); setDraggedEvent(null) }; const remove = () => { if (!selected || selected.type !== 'event') return; mutations.remove.mutate(selected.sourceEntity.id, { onSuccess: () => { setSelected(null); toast.success('Đã xóa sự kiện') }, onError: (error) => toast.error(getApiErrorMessage(error, 'Không thể xóa sự kiện')) }) }; return <div className="calendar-page"><div className="page-heading"><div><p className="eyebrow">LỊCH HỌC TẬP</p><h1>Lịch của bạn</h1><p className="subtle">Lịch học, sự kiện, hạn task và kỳ thi trong một nơi.</p></div><Button onClick={() => openCreate(anchor)}><Plus size={16} /> Tạo sự kiện</Button></div><section className="panel calendar-toolbar"><div className="calendar-nav"><button className="icon-button" onClick={() => move(-1)} aria-label="Khoảng thời gian trước"><ChevronLeft size={18} /></button><button className="calendar-today" onClick={() => setAnchor(new Date())}>Hôm nay</button><button className="icon-button" onClick={() => move(1)} aria-label="Khoảng thời gian sau"><ChevronRight size={18} /></button><strong>{anchor.toLocaleDateString('vi-VN', { month: 'long', year: 'numeric' })}</strong></div><Select value={view} onChange={(event) => setView(event.target.value as CalendarView)} aria-label="Chế độ xem"><option value="day">Ngày</option><option value="week">Tuần</option><option value="month">Tháng</option></Select></section>{query.isLoading ? <Skeleton height={520} /> : query.isError ? <EmptyState title="Không thể tải lịch" description="Kiểm tra kết nối rồi thử lại." action={<Button onClick={() => query.refetch()}>Thử lại</Button>} /> : <section className={`panel calendar-grid calendar-view-${view}`}><div className="calendar-weekdays">{(view === 'day' ? [weekdays[(anchor.getDay() + 6) % 7]] : weekdays).map((label) => <strong key={label}>{label}</strong>)}</div><div className="calendar-cells">{cells.map((cell) => { const items = byDay.get(dayKey(cell)) ?? []; return <div className={`calendar-cell${cell.getMonth() !== anchor.getMonth() && view === 'month' ? ' is-outside' : ''}`} key={dayKey(cell)} onDragOver={(event) => { if (draggedEvent) event.preventDefault() }} onDrop={() => dropEvent(cell)} onDoubleClick={() => openCreate(cell)}><button className="calendar-day" onClick={() => openCreate(cell)}>{cell.getDate()}</button><div className="calendar-items">{items.map((item) => <button key={`${item.type}-${item.sourceEntity.id}-${item.startAt}`} className={`calendar-item calendar-item-${item.type}`} style={{ '--item-color': item.colorHex ?? undefined } as React.CSSProperties} draggable={item.type === 'event'} onDragStart={() => setDraggedEvent(item)} onClick={(event) => { event.stopPropagation(); setSelected(item) }}><span className="calendar-item-dot" /> <span>{item.title}</span><small>{item.type !== 'task_due' && timeLabel(item.startAt)}</small></button>)}</div></div> })}</div></section>}<Modal open={modalOpen} title={editingEvent ? 'Sửa sự kiện' : 'Tạo sự kiện'} onClose={() => setModalOpen(false)} footer={<><Button variant="secondary" onClick={() => setModalOpen(false)}>Hủy</Button><Button type="submit" form="calendar-event-form" loading={mutations.create.isPending || mutations.update.isPending}>{editingEvent ? 'Lưu thay đổi' : 'Lưu sự kiện'}</Button></>}><form id="calendar-event-form" className="modal-form" onSubmit={save}><label className="field"><span>Tên sự kiện</span><input required value={form.title} onChange={(event) => setForm({ ...form, title: event.target.value })} placeholder="Ví dụ: Ôn tập chương 1" /></label><div className="form-grid"><label className="field"><span>Ngày</span><input type="date" required value={form.date} onChange={(event) => setForm({ ...form, date: event.target.value })} /></label><label className="field"><span>Bắt đầu</span><input type="time" required value={form.start} onChange={(event) => setForm({ ...form, start: event.target.value })} /></label></div><label className="field"><span>Kết thúc</span><input type="time" required value={form.end} onChange={(event) => setForm({ ...form, end: event.target.value })} /></label></form></Modal><Modal open={Boolean(selected)} title={selected?.title ?? 'Chi tiết lịch'} onClose={() => setSelected(null)} footer={selected?.type === 'event' ? <><Button variant="secondary" onClick={() => setSelected(null)}>Đóng</Button><Button onClick={editSelected}><Edit3 size={15} /> Sửa</Button><Button variant="danger" onClick={remove} loading={mutations.remove.isPending}><Trash2 size={15} /> Xóa sự kiện</Button></> : <Button onClick={() => setSelected(null)}>Đóng</Button>}><div className="calendar-detail"><span className={`badge ${selected?.type ?? 'blue'}`}>{selected ? labels[selected.type] : ''}</span>{selected && <p><Clock3 size={15} /> {new Date(selected.startAt).toLocaleString('vi-VN', { dateStyle: 'full', timeStyle: 'short' })}</p>}<p className="subtle">{selected?.type === 'task_due' ? 'Đây là hạn hoàn thành của công việc.' : selected?.type === 'exam' ? 'Ngày thi được lấy từ thành phần điểm.' : 'Bạn có thể quản lý sự kiện trong lịch.'}</p></div></Modal></div> }
