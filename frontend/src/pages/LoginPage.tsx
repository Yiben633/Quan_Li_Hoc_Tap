import { useState } from 'react'
import { Navigate, useNavigate } from 'react-router-dom'
import { ArrowRight, GraduationCap } from 'lucide-react'
import { useAuthStore } from '../stores/authStore'
export function LoginPage() { const token = useAuthStore((state) => state.accessToken); const setSession = useAuthStore((state) => state.setSession); const navigate = useNavigate(); const [email, setEmail] = useState(''); if (token) return <Navigate to="/" replace />; return <main className="auth-page"><section className="auth-panel"><div className="auth-logo"><span className="brand-mark"><GraduationCap size={20} /></span>StudyFlow</div><div className="auth-copy"><p className="eyebrow">KHÔNG GIAN HỌC TẬP CỦA BẠN</p><h1>Học có kế hoạch,<br /><em>tiến bộ có nhịp.</em></h1><p>Gom lịch học, công việc và mục tiêu vào một nơi rõ ràng hơn.</p></div><form className="auth-form" onSubmit={(event) => { event.preventDefault(); setSession('demo-access-token', { id: 'demo', fullName: 'Bạn học', email: email || 'student@example.com', roles: ['student'] }); navigate('/') }}><label>Email<input value={email} onChange={(event) => setEmail(event.target.value)} type="email" placeholder="you@example.com" required /></label><label>Mật khẩu<input type="password" placeholder="••••••••" required /></label><button className="button primary wide" type="submit">Đăng nhập <ArrowRight size={17} /></button></form><p className="auth-foot">Chưa có tài khoản? <a href="#register">Đăng ký ngay</a></p></section><aside className="auth-art"><div className="art-card"><span className="art-kicker">HÔM NAY</span><strong>3 việc cần hoàn thành</strong><div className="art-progress"><span style={{ width: '68%' }} /></div><small>68% tiến độ tuần này</small></div><div className="art-note">“Một bước nhỏ mỗi ngày<br />sẽ tạo thành quãng đường dài.”</div></aside></main> }
