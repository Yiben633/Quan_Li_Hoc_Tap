export function ProgressBar({ value, label, tone = 'blue' }: { value: number; label?: string; tone?: 'blue' | 'green' | 'orange' }) { const safeValue = Math.max(0, Math.min(100, value)); return <div className="progress-group">{label && <div className="progress-label"><span>{label}</span><strong>{safeValue}%</strong></div>}<div className="progress-line"><span className={tone} style={{ width: `${safeValue}%` }} /></div></div> }
