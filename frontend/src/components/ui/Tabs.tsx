import type { ReactNode } from 'react'
export function Tabs<T extends string>({ items, value, onChange }: { items: Array<{ value: T; label: ReactNode }>; value: T; onChange: (value: T) => void }) { return <div className="tabs" role="tablist">{items.map((item) => <button key={item.value} className={item.value === value ? 'active' : ''} role="tab" aria-selected={item.value === value} aria-current={item.value === value ? 'page' : undefined} onClick={() => onChange(item.value)}>{item.label}</button>)}</div> }
